version: "3"

services:

  web:

    build: .

    ports:
      - "5000:5000"

    depends_on:
      - mysql

  mysql:

    image: mysql:8

    environment:

      MYSQL_ROOT_PASSWORD: root

      MYSQL_DATABASE: "sales_intelligence"

    ports:
      - "3306:3306"